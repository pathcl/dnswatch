CLAUDE.md
# Compiled binaries at the repo root, not the cmd/ directory.
/dnswatch
/dnswatch-linux-amd64
/dnswatch-linux-amd64.sha256
# BPF bytecode — build artifact produced by clang, embedded into the binary.
internal/capture/dns_capture.o
